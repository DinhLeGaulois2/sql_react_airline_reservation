import React from "react";

import '../../style.scss'

export default class DisplayBookingListComponent extends React.Component {
    constructor(props) {
        super(props);
        this.state = {
        };
    }
    
    componentDidMount() {
    }

    render() {
        return (
            <div>
            </div>
        )
    }
}